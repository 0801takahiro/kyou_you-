<div>
  <div>
    <%= @impression.body %>
  </div>
  <div>
    <div>
      <%= form_with(model:[@impression, @post_impression], local: true) do |f| %>
     <div class="field">
      <%= f.label :返信 %></br>
      <%= f.text_field :body %>
　   </div>
　    <%= f.submit "コメントする" %>
      <% end %>
    </div>
    <% @impression.post_impressions.each do |post_impression| %>
    <div>
     <%= post_impression.body %>
    </div>
    <% end %>
  </div>
</div>